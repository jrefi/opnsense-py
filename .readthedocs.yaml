version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install poetry
    post_install:
      - poetry install --with docs

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
